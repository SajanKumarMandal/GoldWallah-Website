{
  "name": "goldwallah-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "build": "npm run check --prefix backend && npm run build --prefix frontend",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "test:backend": "npm run test --prefix backend",
    "test:frontend": "npm run test --prefix frontend",
    "test:auth": "npm run test:auth --prefix backend && npm run test:auth --prefix frontend"
  }
}

